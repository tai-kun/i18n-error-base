/**
 * ページタイトルです。
 */
const __PAGE_TITLE: string;

/**
 * GitHub リポジトリー名です。
 */
const __REPOSITORY_NAME: string;

/**
 * サポートする言語の一覧です。
 */
const __LOCALES: readonly ["ja", "en"];

/**
 * 言語判定に失敗した際のフォールバック用ロケールです。
 */
const __FALLBACK_LOCALE: "en";

/**
 * 最新のメジャーバージョン番号です。
 */
const __LATEST_MAJOR_VERSION_NUMBER: number;
